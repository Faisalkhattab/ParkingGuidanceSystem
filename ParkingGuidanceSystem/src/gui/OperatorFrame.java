package gui;

import javax.swing.*;

public class OperatorFrame extends JFrame {

    public OperatorFrame() {
        JLabel label = new JLabel("Operator Dashboard - Monitor Parking");
        add(label);
        setSize(300,200);
        setVisible(true);
    }
}
